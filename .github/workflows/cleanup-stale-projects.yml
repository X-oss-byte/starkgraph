
    on : https://github.com/sammyfilly/starkgraph/commit/4ce67cafbab5aa37e72c39f2bacb77ff79ab9e18#annotation_13703283699
  
  workflow_dispatch:
    inputs:
      apply-changes:
        description: 'Whether workflow should generate a pull request for these changes'
        required: false
        default: '1'

name: Cleanup stale projects
jobs:
  cleanupArchivedProjects:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Ruby
        uses: ruby/setup-ruby@250fcd6a742febb1123a77a841497ccaa8b9e939
        with:
          ruby-version: '3.2'
          bundler-cache: true
          cache-version: 0
        run: bundle exec ruby scripts/cleanup_projects.rb
        env:
          GITHUB_TOKEN: ${{ secrets.SHIFTBOT_TOKEN }}
          APPLY_CHANGES: ${{ inputs.apply-changes }}
